LAYOUT=lay/post.dhtml
TITLE=$(grep '^[A-Z][a-z].*' $forig | head -n1)
LINK="Posts"
SUB="Embrace the Unknown"
HEAD="<link href=/$(dirname $forig)/atom.xml type=application/atom+xml rel=alternate title=\"Pickfire $(basename $(dirname $forig)) ATOM Feed\"><link href=/$(dirname $forig)/atom.xml type=application/rss+xml rel=alternate title=\"Pickfire $(basename $(dirname $forig)) RSS Feed\">"
